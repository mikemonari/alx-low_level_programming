#include "main.h"

/**
 * _strcmp - function that compares a string
 * @s1: first parameter
 * @s2: second parameter
 * Return: interger
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
