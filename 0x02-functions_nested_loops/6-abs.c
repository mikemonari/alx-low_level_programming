#include "main.h"
/**
 * main - check the code
 * int _abs -> computes the absolute value of an integer.
 * @n: parameter
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	n = -n;
	return (n);
}
